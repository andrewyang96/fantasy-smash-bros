// Rename to config.js and fill in the blanks
var config = {};

config.recaptchaSecret = "<Google No Captcha Recaptcha Secret>";

config.firebaseSecret = "<Firebase Secret>";

module.exports = config;
